#!/usr/bin/env node

const fs = require('fs');
const path = require('path');

// Parse command line arguments
let network = process.argv[2] || 'crab';

// Show help if requested
if (network === '--help' || network === '-h') {
	console.log('Usage: fix-cache [network]');
	console.log('  network: crab (default) or darwinia');
	console.log('\nExamples:');
	console.log('  fix-cache         # Fix Crab Network cache');
	console.log('  fix-cache crab    # Fix Crab Network cache');
	console.log('  fix-cache darwinia # Fix Darwinia Network cache');
	process.exit(0);
}

const NETWORKS = {
	crab: {
		name: 'Crab',
		cachePath: path.join(__dirname, '..', 'data', 'crab-cache.json'),
		baseUrl: 'https://crab-scan.darwinia.network/api'
	},
	darwinia: {
		name: 'Darwinia',
		cachePath: path.join(__dirname, '..', 'data', 'darwinia-cache.json'),
		baseUrl: 'https://explorer.darwinia.network/api'
	}
};

if (!NETWORKS[network]) {
	console.error(`❌ Error: Invalid network '${network}'`);
	console.log('Usage: fix-cache [network]');
	console.log('  network: crab (default) or darwinia');
	process.exit(1);
}

const config = NETWORKS[network];
const CACHE_PATH = config.cachePath;
const BASE_URL = config.baseUrl;

async function isSmartContract(address) {
	try {
		const url = `${BASE_URL}/v2/smart-contracts/${address}`;
		const response = await fetch(url, {
			headers: { 'Accept': 'application/json' }
		});
		
		if (response.status === 404) {
			return false; // Not a contract
		}
		
		if (!response.ok) {
			return null; // Unknown (API error)
		}
		
		return true; // It's a contract
	} catch (error) {
		return null; // Unknown (network error)
	}
}

async function scanAndFixCache() {
	console.log(`\n🔍 Scanning ${config.name} Network address cache...\n`);
	
	// Load cache
	if (!fs.existsSync(CACHE_PATH)) {
		console.error('❌ Cache file not found:', CACHE_PATH);
		process.exit(1);
	}
	
	const cache = JSON.parse(fs.readFileSync(CACHE_PATH, 'utf-8'));
	const addresses = Object.keys(cache);
	
	console.log(`📊 Total addresses: ${addresses.length}\n`);
	
	const stats = {
		checked: 0,
		correct: 0,
		fixed: 0,
		errors: 0
	};
	
	// Check and fix each address
	for (let i = 0; i < addresses.length; i++) {
		const address = addresses[i];
		const cachedValue = cache[address];
		
		// Show progress
		const progress = Math.round((i + 1) / addresses.length * 100);
		process.stdout.write(`\r⏳ Progress: ${i + 1}/${addresses.length} (${progress}%) | ✅ ${stats.correct} | 🔧 ${stats.fixed} | ❌ ${stats.errors}`);
		
		const actualValue = await isSmartContract(address);
		stats.checked++;
		
		if (actualValue === null) {
			// API/network error
			stats.errors++;
		} else if (cachedValue === actualValue) {
			// Correct
			stats.correct++;
		} else {
			// Incorrect - fix it
			const oldType = cachedValue ? 'Contract' : 'EOA';
			const newType = actualValue ? 'Contract' : 'EOA';
			process.stdout.write(`\n  🔧 Fixing ${address.slice(0, 10)}...${address.slice(-6)}: ${oldType} → ${newType}\n`);
			cache[address] = actualValue;
			stats.fixed++;
		}
		
		// Rate limiting
		await new Promise(r => setTimeout(r, 100));
	}
	
	process.stdout.write('\n\n');
	
	// Save updated cache if fixes were made
	if (stats.fixed > 0) {
		fs.writeFileSync(CACHE_PATH, JSON.stringify(cache, null, 2));
		console.log(`💾 Cache updated with ${stats.fixed} fixes\n`);
	}
	
	// Summary
	console.log('📈 Summary:');
	console.log(`   ✅ Correct:  ${stats.correct}`);
	console.log(`   🔧 Fixed:    ${stats.fixed}`);
	console.log(`   ❌ Errors:   ${stats.errors}`);
	console.log(`   📊 Total:    ${stats.checked}\n`);
	
	if (stats.fixed === 0 && stats.errors === 0) {
		console.log('🎉 All cache entries are correct!\n');
	} else if (stats.fixed > 0) {
		console.log('✅ Cache has been fixed!\n');
	}
	
	if (stats.errors > 0) {
		console.log('⚠️  Some addresses could not be verified due to API/network errors.\n');
	}
}

scanAndFixCache().catch(error => {
	console.error('\n❌ Fatal error:', error.message);
	process.exit(1);
});
